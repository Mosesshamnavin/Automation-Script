(function(){let links=Array.from(document.querySelectorAll('a'));let notes=links.find(e=>{let txt=e.textContent.toLowerCase().trim();if(!txt.startsWith('notes'))return false;let idx=links.indexOf(e);let start=Math.max(0,idx-5);for(let i=start;i<idx;i++){if(links[i].textContent.toLowerCase().trim().startsWith('edit personal data'))return true;}return false;});if(notes){notes.dispatchEvent(new MouseEvent('click',{bubbles:true,cancelable:true,view:window}));notes.click();}})();